import { ProgressBarCart } from "./ProgressBarCart";

export const ProgressBarHome = () => {
    return (
        <>
            <ProgressBarCart width="45%" height="2rem" duration='200' />
        </>
    );
}